/* global Office console */

// This file is intentionally minimal — all business logic has been moved to
// src/taskpane/components/ and src/taskpane/services/
// It is kept so webpack does not break if any import still references it.
export { };
